<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create <%= path %></title>
    <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>
    <div class="flex justify-center">
        <form class="mt-[15vh] bg-amber-50 shadow-lg rounded-md p-6 min-w-md flex flex-col gap-3" action=<%= (data.id) ? `/${path}/${data.id}/edit` :  `/${path}/create` %> method="POST">
            <div>
                <label for="name" class="label">Name</label>
                <input class="form-field" type="text" id="name" name="name" required value="<%= data?.name %>"/>
            </div>
            <div class="mt-4 flex gap-2 justify-end">
                <button type="submit" class="btn btn-primary">Submit</button>
                <a href=<%= "/" + path %> class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</body>
</html>