<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= path %></title>
    <link rel="stylesheet" href="styles/styles.css" />
    <link rel="shortcut icon" href="/coffee.svg" type="image/x-icon" />
  </head>
  <body>
    <div class="flex flex-col items-center">
      <a
        href="/"
        class="flex gap-2 items-center text-sm uppercase text-stone-600 font-bold tracking-wider self-start px-4 hover:underline hover:decoration-2"
        ><svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 20 20"
          width="12px"
          height="12px"
          fill="#57534d"
        >
          <path d="M8 20H3V10H0L10 0l10 10h-3v10h-5v-6H8v6z" /></svg
        >Home</a
      >
      <div
        class="bg-pattern relative grid grid-cols-1 pt-8 gap-6 min-w-xs bg-amber-50 shadow-lg rounded-t-md"
      >
        <div
          class="mt-8 px-8 grid grid-cols-2 gap-8 items-center justify-items-stretch"
        >
          <h1 class="text-lg font-bold text-stone-700 capitalize">
            <%= path %>
          </h1>
          <div class="justify-self-end">
            <a href="<%= `${path}/create` %>" class="btn btn-primary">New</a>
          </div>
        </div>
        <ul role="list">
          <% for (let item of rows) { %>
          <li>
            <a
              href="<%=`/${path}/${item.id}`%>"
              class="block py-4 border-b-2 border-b-orange-100 hover:text-yellow-900 hover:font-bold hover:underline hover:decoration-yellow-900 hover:decoration-2"
              ><div class="px-8"><%= item.name %></div></a
            >
          </li>
          <% } %>
        </ul>
      </div>
    </div>
  </body>
</html>
